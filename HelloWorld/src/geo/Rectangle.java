package geo;

public class Rectangle extends Shape{

    public Rectangle() {
    }

    public Rectangle(String color, int dim, int dimb) {
        super(color, dim, dimb);
    }
    public void area(int dim, int dimb) {
        super.area(dim, dimb);

    }
}
